[project]
name = "kuberay-gangscheduling-demo"
version = "0.1.0"
description = "Ray (kuberay) gang scheduling examples"
requires-python = ">=3.11"

dependencies = [
    "ray[default]==2.56.0",
    "numpy",
]
